> **归档说明**：原文见项目 `_testplan/` 目录；本文件为归档副本（2026-09-17），不再更新。

> **[已存档 2026-09-12]** 本文档已并入权威总纲 `docs/07-测试计划.md`（外部文档），内容仅作历史参考，不再更新。活账本见 `_testplan/findings.md`。

# SuperSampler 验收清单（acceptance）

> 逐条勾选。全绿才视为交付完成。勾选依据：构建输出、`dotnet test` 输出、集成脚本运行 log。

---

## A. 构建

- [ ] `dotnet build SuperSampler.sln -c Debug` 输出 **0 警告 0 错误**
- [ ] Release 构建同绿（`-c Release`）
- [ ] net46 三个产品程序集（Abstractions / Core / [Drivers.Modbus / Hosting]）各自编译通过
- [ ] 测试工程 `SuperSampler.UnitTests`（net462）成功引用产品程序集
- [ ] 门面接口在**签名里不出现内部类型**（Transport / Driver / Block / Scheduler 不出现在 IDeviceManager / IModbusDebugTool）——评审项
- [ ] 每个门面接口方法数 ≤ 10——评审项

## B. 单元测试（全部通过）

- [ ] `dotnet test SuperSampler/tests/SuperSampler.UnitTests`：**0 失败 0 跳过**
- [ ] 原有 25 个单测（Smoke 1 + EventBus 9 + ErrorModel 9 + PointValue 6）**仍全绿**（回归）`[基线]`
- [ ] Codec 全矩阵（4 字序 × 8 类型，unit-tests.md §1.2）全绿
- [ ] Codec 位/位域/缩放/钳制/Map/BCD/字符串/datetime/raw（§1.3–1.10）全绿
- [ ] 配置加载换算 + swap 别名 + 模板/Defaults 继承 + ${i18n}（§2.1–2.4）全绿
- [ ] 校验规则 14 条各有 1 条触发用例且**均正确拒绝**（§2.5）
- [ ] 错误族→Class 8 条、Modbus 异常码 01..0B 全表 + 无应答升级（§3）全绿
- [ ] **写四态**：Succeeded / Failed / Indeterminate / Rejected 各触发用例全绿；`W-CTRL-1`「超时绝不自动重试」通过（安全门）`[P0]`
- [ ] 事件总线补充用例（§5），含 Wait 溢出阻塞、全局 Seq、Dispose 后 Emit
- [ ] PointRegistry / ValueCache / Scheduler / 门面用例（§6–8）全绿

## C. 集成（需 mbserver 环境）

- [ ] mbserver 加载 `supermodbus_sim_tcp.mbs`(:1502) 与 `supermodbus_sim_rtu_tcp.mbs`(:1503) 启动成功，mbclient 手工 poke 全部就绪
- [ ] 场景 1 TCP 基线 15 个子项全过（含 -12345 / 54321 / -123456789 / 3456789012 / -9876543210123 / uint64 max / 3.14159 / 2.718281828459045 / 1.0e12 / -1.0e-12）
- [ ] 场景 2 边界：100 个边界值点（±Inf/NaN/0/空串/max/min）解析正确
- [ ] 场景 3 位/位域/状态字 5 子项过
- [ ] 场景 4 四数据区（保持/输入/线圈/离散）过，线圈离散按公式正确
- [ ] 场景 5 四字序 WO_ABCD/BADC/CDAB/DCBA 用匹配 swap 读出同一逻辑值；原始字四台不同
- [ ] 场景 6 PROCESS 动态：正弦/漂移/计数器递增/随机/泵转速/线圈翻转，两次采样在变且在合理区间
- [ ] 场景 7 RTU-over-TCP 3 从站各点解析正确、不串台
- [ ] 场景 8 大块 C（1248>125）自动分块 FC03 拼接完整；300 点 sin 数组可解析
- [ ] 场景 9-12 异常路径：慢响应 / 超时(unit99 升级 LinkDown) / 写拒绝(READONLY→Failed) / 越界(0x02→Permanent) 全部按预期
- [ ] 场景 13 TriggerRead / RawRead（allowRawAccess 默认关 → 拒绝）过
- [ ] 场景 14 写成功 + verify 回读；边界写按 min/max 拒绝
- [ ] **场景 15 写超时 → Indeterminate 且重试=0**（安全门）`[P0]`
- [ ] 场景 16 多从站多扫描组并发各按节奏刷新、互不串台

## D. 非功能 / 安全 / 质量

- [ ] **Do 基线**：写超时路径验证过「不重发」；Indeterminate 唯一动作是回读 + 事件（W-CTRL-1 / 场景15）
- [ ] 错误聚合：同 Device 同 Kind 时间窗合并去重（docs/02 §7）——若有实现，补对应断言并使验收通过
- [ ] 事件总线：慢订阅者不阻塞采集线程验证过（9 条硬约束）
- [ ] 配置里 `Diagnostics@allowRawAccess` 默认 false，产环境保持关（D26）
- [ ] 评论/文档核对：`MessageKey`/`Code` 跨版本稳定，框架不产出中英文字句

## E. 交付物品

- [ ] 4 份测试文档已生成：`testplan.md` / `unit-tests.md` / `integration-tests.md` / `acceptance.md`
- [ ] 已按「被测模块实现完成度」把 unit-tests.md 标注更新（`[已覆盖]` / `[待新增]`）
- [ ] `[待定]` 项（越界质量、舍入、Block 重叠、报警缺省优先级、双点退化）逐一定稿后回填期望值

---

## 最终验收门

```
[ ] 构建 0 警告 0 错误
[ ] 全部单测（含 25 基线）通过
[ ] 集成 16 场景逐条通过（含两条安全门：写不重试、NaN 不当好值）
[ ] 无任何异常泄露到调度层（SocketException 等必须转 HostError）
```
